apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../pdsl-helm

helmCharts:
  - name: sms-payment-service-dev
    namespace: dev
    releaseName: sms-payment-service
    valuesFile: values-dev.yaml
    additionalValuesFiles:
    - values-image.yaml    
